# Real financial data - NEVER commit (books.db also holds the API key & SMTP password)
data/

# Python
.venv/
__pycache__/
*.pyc

# OS noise
Thumbs.db
desktop.ini

# Test-generated data dirs / stray databases (never commit real or test books)
.test_auto_cat_data/
*.db
